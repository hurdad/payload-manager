services:
  payload-gateway:
    build:
      context: ..
      dockerfile: docker/Dockerfile.gateway
    depends_on:
      payload-manager:
        condition: service_healthy
    environment:
      GRPC_ADDR: payload-manager:50051
      HTTP_ADDR: :8080
    ports:
      - "8080:8080"
