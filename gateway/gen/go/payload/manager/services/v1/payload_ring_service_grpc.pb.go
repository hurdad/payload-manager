// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             (unknown)
// source: payload/manager/services/v1/payload_ring_service.proto

package servicesv1

import (
	context "context"
	v1 "github.com/payload-manager/payload-manager/gateway/gen/go/payload/manager/runtime/v1"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	PayloadRingService_AcquireRingSlot_FullMethodName = "/payload.manager.services.v1.PayloadRingService/AcquireRingSlot"
	PayloadRingService_CommitRingSlot_FullMethodName  = "/payload.manager.services.v1.PayloadRingService/CommitRingSlot"
	PayloadRingService_MapRing_FullMethodName         = "/payload.manager.services.v1.PayloadRingService/MapRing"
	PayloadRingService_LeaseRingSlot_FullMethodName   = "/payload.manager.services.v1.PayloadRingService/LeaseRingSlot"
	PayloadRingService_ReleaseRingSlot_FullMethodName = "/payload.manager.services.v1.PayloadRingService/ReleaseRingSlot"
)

// PayloadRingServiceClient is the client API for PayloadRingService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type PayloadRingServiceClient interface {
	//
	//Reserve the next available slot in `ring_id`. PM picks the slot
	//(round-robin over slots whose refcount has hit zero since they
	//were last committed); the returned (slot_idx, generation) is the
	//triple the producer will write into the downstream event's
	//RingSlotRef.
	//
	//Fails with RESOURCE_EXHAUSTED when no slot is available (every
	//slot is currently leased by at least one consumer) and the ring's
	//exhaustion_policy is DROP_NEW. With BLOCK the call waits.
	AcquireRingSlot(ctx context.Context, in *v1.AcquireRingSlotRequest, opts ...grpc.CallOption) (*v1.AcquireRingSlotResponse, error)
	//
	//Publish the just-written slot. PM marks the slot ready-for-lease
	//so consumers' LeaseRingSlot calls succeed. The producer publishes
	//the downstream domain event (e.g. RadioCaptureEvent) on NATS
	//carrying the RingSlotRef in parallel; consumers see the event
	//and call LeaseRingSlot to take the read lease.
	//
	//Fails with FAILED_PRECONDITION if (slot_idx, generation) doesn't
	//match the producer's last AcquireRingSlot — guards against
	//double-commit or commits on stale handles.
	CommitRingSlot(ctx context.Context, in *v1.CommitRingSlotRequest, opts ...grpc.CallOption) (*v1.CommitRingSlotResponse, error)
	//
	//One-time discovery on consumer startup. Returns the ring's slot
	//count + per-slot shm names so the consumer can mmap (and, for GPU
	//consumers, cudaHostRegister + cudaHostGetDevicePointer) every
	//slot ahead of the hot path. The returned shm names are stable
	//for the lifetime of the PM process; clients should not poll
	//MapRing per-capture.
	//
	//Fails with NOT_FOUND if the ring isn't configured in PM's static
	//config.
	MapRing(ctx context.Context, in *v1.MapRingRequest, opts ...grpc.CallOption) (*v1.MapRingResponse, error)
	//
	//Bump the slot's refcount and grant a read lease. Lightweight —
	//no bytes cross the wire, only the (ring_id, slot_idx, generation)
	//triple and the returned opaque lease_id.
	//
	//Fails with FAILED_PRECONDITION if the generation no longer matches
	//the slot's current generation (consumer was too slow, producer
	//already recycled the slot). The consumer should skip this capture
	//and wait for the next event.
	LeaseRingSlot(ctx context.Context, in *v1.LeaseRingSlotRequest, opts ...grpc.CallOption) (*v1.LeaseRingSlotResponse, error)
	//
	//Drop the lease, decrement the slot's refcount. When the refcount
	//reaches zero the slot is eligible for the producer's next
	//AcquireRingSlot. Idempotent: releasing an already-released lease
	//is a no-op.
	ReleaseRingSlot(ctx context.Context, in *v1.ReleaseRingSlotRequest, opts ...grpc.CallOption) (*emptypb.Empty, error)
}

type payloadRingServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewPayloadRingServiceClient(cc grpc.ClientConnInterface) PayloadRingServiceClient {
	return &payloadRingServiceClient{cc}
}

func (c *payloadRingServiceClient) AcquireRingSlot(ctx context.Context, in *v1.AcquireRingSlotRequest, opts ...grpc.CallOption) (*v1.AcquireRingSlotResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(v1.AcquireRingSlotResponse)
	err := c.cc.Invoke(ctx, PayloadRingService_AcquireRingSlot_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *payloadRingServiceClient) CommitRingSlot(ctx context.Context, in *v1.CommitRingSlotRequest, opts ...grpc.CallOption) (*v1.CommitRingSlotResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(v1.CommitRingSlotResponse)
	err := c.cc.Invoke(ctx, PayloadRingService_CommitRingSlot_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *payloadRingServiceClient) MapRing(ctx context.Context, in *v1.MapRingRequest, opts ...grpc.CallOption) (*v1.MapRingResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(v1.MapRingResponse)
	err := c.cc.Invoke(ctx, PayloadRingService_MapRing_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *payloadRingServiceClient) LeaseRingSlot(ctx context.Context, in *v1.LeaseRingSlotRequest, opts ...grpc.CallOption) (*v1.LeaseRingSlotResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(v1.LeaseRingSlotResponse)
	err := c.cc.Invoke(ctx, PayloadRingService_LeaseRingSlot_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *payloadRingServiceClient) ReleaseRingSlot(ctx context.Context, in *v1.ReleaseRingSlotRequest, opts ...grpc.CallOption) (*emptypb.Empty, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(emptypb.Empty)
	err := c.cc.Invoke(ctx, PayloadRingService_ReleaseRingSlot_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// PayloadRingServiceServer is the server API for PayloadRingService service.
// All implementations must embed UnimplementedPayloadRingServiceServer
// for forward compatibility.
type PayloadRingServiceServer interface {
	//
	//Reserve the next available slot in `ring_id`. PM picks the slot
	//(round-robin over slots whose refcount has hit zero since they
	//were last committed); the returned (slot_idx, generation) is the
	//triple the producer will write into the downstream event's
	//RingSlotRef.
	//
	//Fails with RESOURCE_EXHAUSTED when no slot is available (every
	//slot is currently leased by at least one consumer) and the ring's
	//exhaustion_policy is DROP_NEW. With BLOCK the call waits.
	AcquireRingSlot(context.Context, *v1.AcquireRingSlotRequest) (*v1.AcquireRingSlotResponse, error)
	//
	//Publish the just-written slot. PM marks the slot ready-for-lease
	//so consumers' LeaseRingSlot calls succeed. The producer publishes
	//the downstream domain event (e.g. RadioCaptureEvent) on NATS
	//carrying the RingSlotRef in parallel; consumers see the event
	//and call LeaseRingSlot to take the read lease.
	//
	//Fails with FAILED_PRECONDITION if (slot_idx, generation) doesn't
	//match the producer's last AcquireRingSlot — guards against
	//double-commit or commits on stale handles.
	CommitRingSlot(context.Context, *v1.CommitRingSlotRequest) (*v1.CommitRingSlotResponse, error)
	//
	//One-time discovery on consumer startup. Returns the ring's slot
	//count + per-slot shm names so the consumer can mmap (and, for GPU
	//consumers, cudaHostRegister + cudaHostGetDevicePointer) every
	//slot ahead of the hot path. The returned shm names are stable
	//for the lifetime of the PM process; clients should not poll
	//MapRing per-capture.
	//
	//Fails with NOT_FOUND if the ring isn't configured in PM's static
	//config.
	MapRing(context.Context, *v1.MapRingRequest) (*v1.MapRingResponse, error)
	//
	//Bump the slot's refcount and grant a read lease. Lightweight —
	//no bytes cross the wire, only the (ring_id, slot_idx, generation)
	//triple and the returned opaque lease_id.
	//
	//Fails with FAILED_PRECONDITION if the generation no longer matches
	//the slot's current generation (consumer was too slow, producer
	//already recycled the slot). The consumer should skip this capture
	//and wait for the next event.
	LeaseRingSlot(context.Context, *v1.LeaseRingSlotRequest) (*v1.LeaseRingSlotResponse, error)
	//
	//Drop the lease, decrement the slot's refcount. When the refcount
	//reaches zero the slot is eligible for the producer's next
	//AcquireRingSlot. Idempotent: releasing an already-released lease
	//is a no-op.
	ReleaseRingSlot(context.Context, *v1.ReleaseRingSlotRequest) (*emptypb.Empty, error)
	mustEmbedUnimplementedPayloadRingServiceServer()
}

// UnimplementedPayloadRingServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedPayloadRingServiceServer struct{}

func (UnimplementedPayloadRingServiceServer) AcquireRingSlot(context.Context, *v1.AcquireRingSlotRequest) (*v1.AcquireRingSlotResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method AcquireRingSlot not implemented")
}
func (UnimplementedPayloadRingServiceServer) CommitRingSlot(context.Context, *v1.CommitRingSlotRequest) (*v1.CommitRingSlotResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method CommitRingSlot not implemented")
}
func (UnimplementedPayloadRingServiceServer) MapRing(context.Context, *v1.MapRingRequest) (*v1.MapRingResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method MapRing not implemented")
}
func (UnimplementedPayloadRingServiceServer) LeaseRingSlot(context.Context, *v1.LeaseRingSlotRequest) (*v1.LeaseRingSlotResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method LeaseRingSlot not implemented")
}
func (UnimplementedPayloadRingServiceServer) ReleaseRingSlot(context.Context, *v1.ReleaseRingSlotRequest) (*emptypb.Empty, error) {
	return nil, status.Error(codes.Unimplemented, "method ReleaseRingSlot not implemented")
}
func (UnimplementedPayloadRingServiceServer) mustEmbedUnimplementedPayloadRingServiceServer() {}
func (UnimplementedPayloadRingServiceServer) testEmbeddedByValue()                            {}

// UnsafePayloadRingServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to PayloadRingServiceServer will
// result in compilation errors.
type UnsafePayloadRingServiceServer interface {
	mustEmbedUnimplementedPayloadRingServiceServer()
}

func RegisterPayloadRingServiceServer(s grpc.ServiceRegistrar, srv PayloadRingServiceServer) {
	// If the following call panics, it indicates UnimplementedPayloadRingServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&PayloadRingService_ServiceDesc, srv)
}

func _PayloadRingService_AcquireRingSlot_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(v1.AcquireRingSlotRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(PayloadRingServiceServer).AcquireRingSlot(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: PayloadRingService_AcquireRingSlot_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(PayloadRingServiceServer).AcquireRingSlot(ctx, req.(*v1.AcquireRingSlotRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _PayloadRingService_CommitRingSlot_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(v1.CommitRingSlotRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(PayloadRingServiceServer).CommitRingSlot(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: PayloadRingService_CommitRingSlot_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(PayloadRingServiceServer).CommitRingSlot(ctx, req.(*v1.CommitRingSlotRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _PayloadRingService_MapRing_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(v1.MapRingRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(PayloadRingServiceServer).MapRing(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: PayloadRingService_MapRing_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(PayloadRingServiceServer).MapRing(ctx, req.(*v1.MapRingRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _PayloadRingService_LeaseRingSlot_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(v1.LeaseRingSlotRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(PayloadRingServiceServer).LeaseRingSlot(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: PayloadRingService_LeaseRingSlot_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(PayloadRingServiceServer).LeaseRingSlot(ctx, req.(*v1.LeaseRingSlotRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _PayloadRingService_ReleaseRingSlot_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(v1.ReleaseRingSlotRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(PayloadRingServiceServer).ReleaseRingSlot(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: PayloadRingService_ReleaseRingSlot_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(PayloadRingServiceServer).ReleaseRingSlot(ctx, req.(*v1.ReleaseRingSlotRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// PayloadRingService_ServiceDesc is the grpc.ServiceDesc for PayloadRingService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var PayloadRingService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "payload.manager.services.v1.PayloadRingService",
	HandlerType: (*PayloadRingServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "AcquireRingSlot",
			Handler:    _PayloadRingService_AcquireRingSlot_Handler,
		},
		{
			MethodName: "CommitRingSlot",
			Handler:    _PayloadRingService_CommitRingSlot_Handler,
		},
		{
			MethodName: "MapRing",
			Handler:    _PayloadRingService_MapRing_Handler,
		},
		{
			MethodName: "LeaseRingSlot",
			Handler:    _PayloadRingService_LeaseRingSlot_Handler,
		},
		{
			MethodName: "ReleaseRingSlot",
			Handler:    _PayloadRingService_ReleaseRingSlot_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "payload/manager/services/v1/payload_ring_service.proto",
}
